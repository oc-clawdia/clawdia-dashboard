[build]
  publish = "public"

[build.environment]
  NODE_VERSION = "18"

[[headers]]
  for = "/*"
  [headers.values]
    X-Robots-Tag = "noindex"
    
[context.production]
  command = "echo 'No build step needed for static site'"